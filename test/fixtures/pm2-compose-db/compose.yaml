services:
  postgres:
    image: postgres:16.8
    expose: ["5432"]
    environment:
      POSTGRES_USER: app
      POSTGRES_DB: app
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?set by DeployKit}
    volumes:
      - postgres-data:/var/lib/postgresql/data
volumes:
  postgres-data:
